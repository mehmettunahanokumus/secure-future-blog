---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ThemeToggle from '../components/ThemeToggle.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
).slice(0, 3);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<ThemeToggle />
		<Header />
		<main>
			<div class="hero-section fade-in">
				<div class="hero-content">
					<h1 class="hero-title">Securing the Digital Future</h1>
					<p class="hero-subtitle">
						Insights on cybersecurity, emerging technologies, and the evolving threat landscape. 
						Exploring how to build resilient systems and stay ahead of tomorrow's challenges.
					</p>
					<div class="hero-actions">
						<a href="/blog" class="btn btn-primary">Read Articles</a>
						<a href="/about" class="btn btn-secondary">About Me</a>
					</div>
				</div>
			</div>
			
			<section class="about-section">
				<h2 class="section-title">About Me</h2>
				<div class="about-content">
					<div class="about-text">
						<p>
							I work in cybersecurity and information security, but it’s more than just a job for me. I
							genuinely enjoy digging into how technology works, where it’s headed, and how it can go
							wrong. I’m the kind of person who falls down rabbit holes on AI models, security tools, or
							some weird tech concept I randomly come across, just because I want to understand it.
						</p>
						<p>
							This blog is basically my place to think out loud. I write about cybersecurity, technology, AI,
							and whatever else I’m exploring at the moment. Some posts are practical, some are just me
							connecting ideas, and some are there because I felt like chasing a thought until it made
							sense.
						</p>
						<p>
							If you’re into tech, learning, or just following someone’s curiosity in real time, you’ll feel at
							home here. And even if you’re not, at least you’ll get an honest look at how I see the world
							and the field I work in.
						</p>
						<div class="about-stats">
							<div class="stat-item">
								<span class="stat-number">50+</span>
								<span class="stat-label">Projects Completed</span>
							</div>
							<div class="stat-item">
								<span class="stat-number">3+</span>
								<span class="stat-label">Years Experience</span>
							</div>
							<div class="stat-item">
								<span class="stat-number">100+</span>
								<span class="stat-label">Cups of Coffee</span>
							</div>
						</div>
					</div>
				</div>
			</section>

			<section class="latest-posts-section">
				<h2 class="section-title">Latest Posts</h2>
				<div class="latest-posts-grid">
					{
						posts.map((post) => (
							<article class="latest-card">
								<a href={`/blog/${post.id}/`} class="latest-link">
									<div class="latest-image">
										<img src={post.data.heroImage} alt={post.data.title} loading="lazy" />
									</div>
									<div class="latest-content">
										<h3>{post.data.title}</h3>
										<p class="desc">{post.data.description}</p>
										<div class="meta"><FormattedDate date={post.data.pubDate} /></div>
									</div>
								</a>
							</article>
						))
					}
				</div>
			</section>
			

		</main>
		<Footer />
	</body>
</html>

<style>
	.hero-section {
		text-align: center;
		padding: 4rem 0 6rem;
		background: linear-gradient(135deg, var(--background) 0%, var(--background-secondary) 100%);
		border-radius: var(--radius-xl);
		margin-bottom: 4rem;
	}
	
	.hero-content {
		max-width: 800px;
		margin: 0 auto;
		padding: 0 2rem;
	}
	
	.hero-title {
		font-size: clamp(2.5rem, 6vw, 4rem);
		font-weight: 800;
		margin-bottom: 1.5rem;
		background: linear-gradient(135deg, var(--accent) 0%, var(--accent-dark) 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		line-height: 1.1;
	}
	
	.hero-subtitle {
		font-size: 1.25rem;
		color: var(--text-secondary);
		margin-bottom: 2.5rem;
		line-height: 1.6;
	}
	
	.hero-actions {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}
	
	.btn {
		display: inline-flex;
		align-items: center;
		padding: 0.875rem 2rem;
		border-radius: var(--radius-lg);
		text-decoration: none;
		font-weight: 600;
		font-size: 1rem;
		transition: var(--transition);
		border: 2px solid transparent;
	}
	
	.btn-primary {
		background: var(--accent);
		color: white;
	}
	
	.btn-primary:hover {
		background: var(--accent-dark);
		transform: translateY(-2px);
		box-shadow: var(--shadow-lg);
		text-decoration: none;
	}
	
	.btn-secondary {
		background: transparent;
		color: var(--text-primary);
		border-color: var(--border);
	}
	
	.btn-secondary:hover {
		background: var(--background-secondary);
		border-color: var(--accent);
		color: var(--accent);
		transform: translateY(-2px);
		text-decoration: none;
	}
	
	.section-title {
		text-align: center;
		margin-bottom: 3rem;
		font-size: 2.5rem;
		font-weight: 700;
	}
	
	.about-section {
		margin-bottom: 5rem;
	}
	
	.about-content {
		max-width: 800px;
		margin: 0 auto;
	}
	
	.about-text {
		background: var(--background-secondary);
		padding: 3rem;
		border-radius: var(--radius-xl);
		border: 1px solid var(--border);
		box-shadow: var(--shadow-sm);
	}
	
	.about-text p {
		font-size: 1.125rem;
		line-height: 1.7;
		margin-bottom: 1.5rem;
		color: var(--text-secondary);
	}
	
	.about-text p:last-of-type {
		margin-bottom: 2rem;
	}
	
	.about-stats {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
		gap: 2rem;
		margin-top: 2rem;
		padding-top: 2rem;
		border-top: 1px solid var(--border);
	}

	.latest-posts-section {
		margin-top: 3rem;
		margin-bottom: 5rem;
	}

	.latest-posts-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
		gap: 1.5rem;
		max-width: 1100px;
		margin: 0 auto;
	}

	.latest-card {
		background: var(--background-secondary);
		border: 1px solid var(--border);
		border-radius: var(--radius-lg);
		overflow: hidden;
		transition: var(--transition);
	}

	.latest-card:hover {
		transform: translateY(-4px);
		box-shadow: var(--shadow-lg);
		border-color: var(--accent-light);
	}

	.latest-link { display: block; color: inherit; text-decoration: none; }

	.latest-image { aspect-ratio: 16 / 9; overflow: hidden; }
	.latest-image img { width: 100%; height: 100%; object-fit: cover; display: block; }

	.latest-content { padding: 1rem; }

	.latest-content h3 { margin: 0 0 0.5rem 0; font-size: 1.1rem; color: var(--text-primary); }
	.latest-content .desc { margin: 0; color: var(--text-secondary); font-size: 0.95rem; }
	.latest-content .meta { margin-top: 0.75rem; color: var(--text-tertiary); font-size: 0.85rem; }
	
	.stat-item {
		text-align: center;
	}
	
	.stat-number {
		display: block;
		font-size: 2rem;
		font-weight: 700;
		color: var(--accent);
		margin-bottom: 0.5rem;
	}
	
	.stat-label {
		font-size: 0.875rem;
		color: var(--text-tertiary);
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-weight: 500;
	}
	

	
	@media (max-width: 768px) {
		.hero-section {
			padding: 3rem 0 4rem;
			margin-bottom: 3rem;
		}
		
		.hero-actions {
			flex-direction: column;
			align-items: center;
		}
		
		.btn {
			width: 100%;
			max-width: 280px;
			justify-content: center;
		}
		
		.about-text {
			padding: 2rem;
		}
		
		.about-text p {
			font-size: 1rem;
		}
		
		.about-stats {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}
		
		.steps-grid {
			grid-template-columns: 1fr;
		}
		
		.section-title {
			font-size: 2rem;
		}
	}
</style>
